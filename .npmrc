# react-18-image-lightbox declares a React 16/17/18 peer range but works on React 19.
# Without this a clean `npm install` fails with ERESOLVE for every new developer and CI.
# Remove once the lightbox is swapped for a React 19 compatible one.
legacy-peer-deps=true
